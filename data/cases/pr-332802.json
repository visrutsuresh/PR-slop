{
  "input": {
    "number": 332802,
    "title": "nes: apply supportsUnifiedCompletions on the default provider path",
    "body": "## Summary\n\nFollow-up hotfix to #332018. The `supportsUnifiedCompletions` capability (baked into the `patchBased02Optimized` prompting strategy) was only wired into the **joint** completions provider (`JointCompletionsProviderContribution`), which is gated behind `chat.advanced.inlineEdits.jointCompletionsProvider.enabled` (default **`false`**).\n\nOn the **default** provider path, `InlineEditProviderFeature.registerProvider()` registers NES and suppresses the separate completions/ghost-text provider via a `modelUnification`-derived `excludes` list. Because that block only consulted the core `unificationStateValue?.modelUnification`, a model that sets `supportsUnifiedCompletions` purely through its prompting strategy (e.g. a `copilot-proxy` `/models` deployment with no ExP/local unification toggles) still emitted ghost-text completions.\n\n## Fix\n\nDerive the effective unification in `InlineEditProviderFeature` from the baked `supportsUnifiedCompletions` flag, OR-ed with the core `modelUnification` state, and use it to gate the `excludes`:\n\n```ts\nconst modelUnification = this._supportsUnifiedCompletions.read(reader) || (unificationStateValue?.modelUnification ?? false);\n```\n\nWhen set, both `'completions'` and `'github.copilot'` are excluded -- matching the `excludedProviders: \"completions,github.copilot\"` behavior the strategy is meant to replace. The flag is observed reactively via `IInlineEditsModelService.onModelListUpdated`, mirroring the wiring already added to the joint provider in #332018. The core experiment/deployment path is unchanged for models that do not set the flag.\n\n## Known limitations\n\n- **Startup flicker (fetched `/models` path only).** `supportsUnifiedCompletions` is the only strategy-baked value read at provider-registration time rather than per-request, so it depends on the async `/models` resolution. During the cold-start window before the fetch resolves, `selectedModelConfiguration()` has no strategy yet, the flag reads `false`, and the separate completions provider is not yet excluded -- so ghost text can briefly appear. It self-corrects the instant `onModelListUpdated` fires (the observable has no debounce and re-registers immediately). This does **not** affect local/ExP model configuration, which resolves synchronously at startup. Fully eliminating the window would require holding the separate `github.copilot` completions provider until the NES model list first resolves; that is a larger change tracked in #332837 and intentionally left out of this hotfix.\n- **`forceUseDefaultModel` fallback drops baked knobs (pre-existing, all strategies).** If the deployed model returns `NotFound` (404), the provider falls back to `defaultModelConfiguration()`, which does not apply `applyStrategyConfig`. All strategy-baked values are bypassed under this fallback -- this is shared graceful-degradation behavior affecting every strategy (including `patchBased02WithRecentLineNumbers`), not specific to `patchBased02Optimized`.\n\n## Testing\n\n- `npm run typecheck --prefix extensions/copilot` (changed file clean)\n- focused Vitest for `applyStrategyConfig` / `MODEL_CONFIGURATION_VALIDATOR` and `UserInteractionMonitor`\n- `npm run precommit`\n\n",
    "changed_files": [
      "extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts"
    ],
    "patch": "From 409a2bfb6bb2fa42da2297183fc1af25b258fafd Mon Sep 17 00:00:00 2001\nFrom: Julia Gong <juliagong@microsoft.com>\nDate: Wed, 26 Aug 2026 11:43:17 -0700\nSubject: [PATCH 1/3] Apply supportsUnifiedCompletions to the default provider\n path\n\nThe completions/ghost-text suppression driven by supportsUnifiedCompletions\nwas only wired into the joint completions provider, which is gated behind\nInlineEditsJointCompletionsProviderEnabled (default false). On the default\npath, InlineEditProviderFeature registers NES and suppresses completions via\nthe modelUnification-derived excludes, so ghost text kept firing when only\nthe prompting strategy was set.\n\nDerive the effective unification in InlineEditProviderFeature from the baked\nsupportsUnifiedCompletions flag (OR-ed with the core modelUnification state),\nso both 'completions' and 'github.copilot' are excluded purely from the\nselected model's strategy, matching the excludedProviders behavior.\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\nCopilot-Session: 676b2f6f-4b80-4754-8dde-708d7e992148\n---\n .../inlineEdits/vscode-node/inlineEditProviderFeature.ts | 9 ++++++++-\n 1 file changed, 8 insertions(+), 1 deletion(-)\n\ndiff --git a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\nindex 7f9c6de770102..eed306c21e0af 100644\n--- a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n+++ b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n@@ -9,6 +9,7 @@ import { ConfigKey, IConfigurationService } from '../../../platform/configuratio\n import { IEnvService } from '../../../platform/env/common/envService';\n import { IVSCodeExtensionContext } from '../../../platform/extContext/common/extensionContext';\n import { InlineEditRequestLogContext } from '../../../platform/inlineEdits/common/inlineEditLogContext';\n+import { IInlineEditsModelService } from '../../../platform/inlineEdits/common/inlineEditsModelService';\n import { ObservableGit } from '../../../platform/inlineEdits/common/observableGit';\n import { NesHistoryContextProvider } from '../../../platform/inlineEdits/common/workspaceEditTracker/nesHistoryContextProvider';\n import { ILogService } from '../../../platform/log/common/logService';\n@@ -63,6 +64,7 @@ export class InlineEditProviderFeature {\n \tprivate readonly _yieldToCopilot = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsYieldToCopilot, this._expService);\n \tprivate readonly _excludedProviders = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsExcludedProviders, this._expService).map(v => v ? v.split(',').map(v => v.trim()).filter(v => v !== '') : []);\n \tprivate readonly _copilotToken = observableFromEvent(this, this._authenticationService.onDidCopilotTokenChange, () => this._authenticationService.copilotToken);\n+\tprivate readonly _supportsUnifiedCompletions = observableFromEvent(this, this._modelService.onModelListUpdated, () => this._modelService.selectedModelConfiguration().supportsUnifiedCompletions ?? false);\n \n \tpublic readonly inlineEditsEnabled = derived(this, (reader) => {\n \t\tconst copilotToken = this._copilotToken.read(reader);\n@@ -92,6 +94,7 @@ export class InlineEditProviderFeature {\n \t\t@IExperimentationService private readonly _expService: IExperimentationService,\n \t\t@IEnvService private readonly _envService: IEnvService,\n \t\t@IInstantiationService private readonly _instantiationService: IInstantiationService,\n+\t\t@IInlineEditsModelService private readonly _modelService: IInlineEditsModelService,\n \t) {\n \t}\n \n@@ -158,8 +161,12 @@ export class InlineEditProviderFeature {\n \t\t\tconst provider = this._instantiationService.createInstance(InlineCompletionProviderImpl, model, logger, logContextRecorder, inlineEditDebugComponent, telemetrySender, expectedEditCaptureController);\n \n \t\t\tconst unificationStateValue = unificationState.read(reader);\n+\t\t\t// A model whose strategy bakes in `supportsUnifiedCompletions` runs as the single unified\n+\t\t\t// provider: this stands in for the `modelUnification` deployment toggle so the behavior can\n+\t\t\t// be driven purely from the selected model's prompting strategy.\n+\t\t\tconst modelUnification = this._supportsUnifiedCompletions.read(reader) || (unificationStateValue?.modelUnification ?? false);\n \t\t\tlet excludes = this._excludedProviders.read(reader);\n-\t\t\tif (unificationStateValue?.modelUnification) {\n+\t\t\tif (modelUnification) {\n \t\t\t\texcludes = excludes.slice(0);\n \t\t\t\tif (!excludes.includes('completions')) {\n \t\t\t\t\texcludes.push('completions');\n\nFrom 8f650d68f9b858dafe35236537622625f1655210 Mon Sep 17 00:00:00 2001\nFrom: Julia Gong <juliagong@microsoft.com>\nDate: Wed, 26 Aug 2026 12:31:35 -0700\nSubject: [PATCH 2/3] Document supportsUnifiedCompletions startup-flicker\n caveat\n\nAdd a code comment at the reactive read site explaining the brief\ncold-start window on the fetched /models path where the separate\ncompletions provider is not yet excluded. Matches the Known limitations\nsection in the PR description.\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\nCopilot-Session: 676b2f6f-4b80-4754-8dde-708d7e992148\n---\n .../inlineEdits/vscode-node/inlineEditProviderFeature.ts     | 5 +++++\n 1 file changed, 5 insertions(+)\n\ndiff --git a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\nindex eed306c21e0af..deacf7558748a 100644\n--- a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n+++ b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n@@ -64,6 +64,11 @@ export class InlineEditProviderFeature {\n \tprivate readonly _yieldToCopilot = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsYieldToCopilot, this._expService);\n \tprivate readonly _excludedProviders = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsExcludedProviders, this._expService).map(v => v ? v.split(',').map(v => v.trim()).filter(v => v !== '') : []);\n \tprivate readonly _copilotToken = observableFromEvent(this, this._authenticationService.onDidCopilotTokenChange, () => this._authenticationService.copilotToken);\n+\t// Read reactively because this is the only strategy-baked value consumed at provider-registration\n+\t// time. For a fetched `/models` deployment it resolves asynchronously, so there is a brief cold-start\n+\t// window (typically the `/models` round-trip, ~100ms) where this reads `false` and the separate\n+\t// completions provider is not yet excluded. It self-corrects the instant `onModelListUpdated` fires.\n+\t// Local/ExP model configuration resolves synchronously, so it is unaffected.\n \tprivate readonly _supportsUnifiedCompletions = observableFromEvent(this, this._modelService.onModelListUpdated, () => this._modelService.selectedModelConfiguration().supportsUnifiedCompletions ?? false);\n \n \tpublic readonly inlineEditsEnabled = derived(this, (reader) => {\n\nFrom 4e785726c0bbe4e4f04a6bea5769e1f7f2247686 Mon Sep 17 00:00:00 2001\nFrom: Julia Gong <juliagong@microsoft.com>\nDate: Wed, 26 Aug 2026 12:36:02 -0700\nSubject: [PATCH 3/3] Trim unified-completions comments to two lines\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>\nCopilot-Session: 676b2f6f-4b80-4754-8dde-708d7e992148\n---\n .../vscode-node/inlineEditProviderFeature.ts         | 12 ++++--------\n 1 file changed, 4 insertions(+), 8 deletions(-)\n\ndiff --git a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\nindex deacf7558748a..73c04d30515e7 100644\n--- a/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n+++ b/extensions/copilot/src/extension/inlineEdits/vscode-node/inlineEditProviderFeature.ts\n@@ -64,11 +64,8 @@ export class InlineEditProviderFeature {\n \tprivate readonly _yieldToCopilot = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsYieldToCopilot, this._expService);\n \tprivate readonly _excludedProviders = this._configurationService.getExperimentBasedConfigObservable(ConfigKey.TeamInternal.InlineEditsExcludedProviders, this._expService).map(v => v ? v.split(',').map(v => v.trim()).filter(v => v !== '') : []);\n \tprivate readonly _copilotToken = observableFromEvent(this, this._authenticationService.onDidCopilotTokenChange, () => this._authenticationService.copilotToken);\n-\t// Read reactively because this is the only strategy-baked value consumed at provider-registration\n-\t// time. For a fetched `/models` deployment it resolves asynchronously, so there is a brief cold-start\n-\t// window (typically the `/models` round-trip, ~100ms) where this reads `false` and the separate\n-\t// completions provider is not yet excluded. It self-corrects the instant `onModelListUpdated` fires.\n-\t// Local/ExP model configuration resolves synchronously, so it is unaffected.\n+\t// Read reactively: on a fetched `/models` deployment this resolves async, so a brief cold-start\n+\t// window can emit completions until `onModelListUpdated` fires and re-registers with the excludes.\n \tprivate readonly _supportsUnifiedCompletions = observableFromEvent(this, this._modelService.onModelListUpdated, () => this._modelService.selectedModelConfiguration().supportsUnifiedCompletions ?? false);\n \n \tpublic readonly inlineEditsEnabled = derived(this, (reader) => {\n@@ -166,9 +163,8 @@ export class InlineEditProviderFeature {\n \t\t\tconst provider = this._instantiationService.createInstance(InlineCompletionProviderImpl, model, logger, logContextRecorder, inlineEditDebugComponent, telemetrySender, expectedEditCaptureController);\n \n \t\t\tconst unificationStateValue = unificationState.read(reader);\n-\t\t\t// A model whose strategy bakes in `supportsUnifiedCompletions` runs as the single unified\n-\t\t\t// provider: this stands in for the `modelUnification` deployment toggle so the behavior can\n-\t\t\t// be driven purely from the selected model's prompting strategy.\n+\t\t\t// Unify when the selected model's strategy bakes in `supportsUnifiedCompletions`, or when the\n+\t\t\t// core deployment/ExP `modelUnification` toggle is set.\n \t\t\tconst modelUnification = this._supportsUnifiedCompletions.read(reader) || (unificationStateValue?.modelUnification ?? false);\n \t\t\tlet excludes = this._excludedProviders.read(reader);\n \t\t\tif (modelUnification) {\n"
  },
  "truth": {
    "number": 332802,
    "bucket": 2,
    "merged": true,
    "closed_at": "2026-08-29T13:29:40Z",
    "created_at": "2026-08-26T18:45:55Z",
    "author_pseudonym": "AUTHOR-f0afe151",
    "author_association": "CONTRIBUTOR",
    "is_bot": false,
    "link_declared": false,
    "closing_ref_forms": [],
    "referenced_issues": []
  }
}
