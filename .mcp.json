{
  "_comment": "Relative on purpose: this file is committed, and an absolute path would name one machine. Works as-is for `cd PR-slop && claude`. From anywhere else run ./run.sh mcp-install, which rewrites this with the absolute path of your clone.",
  "mcpServers": {
    "pr-slop": {
      "command": "python3",
      "args": ["src/mcp_server.py"]
    }
  }
}
